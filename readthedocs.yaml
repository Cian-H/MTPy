version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.14"
  jobs:
    pre_create_environment:
      - pip install uv
    create_environment:
      - uv sync --group docs
    install:
      - uv run properdocs build --site-dir $READTHEDOCS_OUTPUT/html
